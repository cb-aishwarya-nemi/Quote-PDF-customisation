/** Max width of the quote document on the prompt builder canvas */
export const CANVAS_DOCUMENT_MAX_WIDTH = 840

/** Shared workspace background for canvas and data-mapping views */
export const BUILDER_WORKSPACE_BG = "#e8eaed"

/** Fixed row height shared by workflow tabs and Chargebee assistant headers. */
export const BUILDER_STRIP_HEIGHT_CLASS = "h-10"

/** Shared panel header row chrome (height applied separately). */
export const BUILDER_PANEL_HEADER_CLASS =
  "flex shrink-0 items-center border-b border-gray-200"

/** Offset for floating canvas controls when workflow tabs are visible. */
export const BUILDER_WORKFLOW_TABS_OFFSET_CLASS = "top-10"

/** Width of the pages panel column */
export const PAGES_PANEL_WIDTH_CLASS = "w-[124px]"

/** Uniform inset inside the white document shell */
export const CANVAS_DOCUMENT_PADDING_PX = 40

/** CSS variable set on `TemplateDocumentFrame` for full-bleed block backgrounds */
export const CANVAS_DOCUMENT_PADDING_VAR = "var(--canvas-doc-padding)"
